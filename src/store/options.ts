export interface SVGElement {
  id: string;
  path?: string;
  fill: string;
  stroke: string;
  strokeWidth: number;
}

export interface Style {
  fill: string;
  strokeWidth: number;
  stroke: string;
}
