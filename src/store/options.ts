export interface Element {
  id: string;
  path?: string;
  fill: string;
  stroke: string;
  strokeWidth: number;
}

export interface Style {
  fill: string;
  strokeWidth: number;
  stroke: string;
}
